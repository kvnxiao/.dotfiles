interface:
  display_name: "Audit Prose"
  short_description: "Audit and rewrite technical prose for clarity and verified meaning."
  default_prompt: "Use $audit-prose to audit this prose."

policy:
  allow_implicit_invocation: true
