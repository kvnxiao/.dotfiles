# Git 2.54+ config-based hooks for this repo.
# Wire into a fresh clone with: just setup-hooks
# (or manually: git config --local --add include.path ../.githooks/config)

[hook "dprint-fmt-staged"]
	event = pre-commit
	command = sh .githooks/pre-commit-dprint-fmt.sh
